confs:
  Chart@v0.0.1:
    rikamiVersion: 0.2.10
    appName: rikami-api

envs:
  - prod:
      WebServer@v0.0.1:
        image: ghcr.io/b-zago/rikami-api/webserver:af4e2b1
        runsOn: golang
        port: 8080
        Secret@v0.0.1:
          data: "(( secFile `.env.web.server.envs.secret` | nindent 6 ))"
        deployment:
          envSecretRefs:
            - "(( .Shards.WebServer.secret.name ))" 
      Database@v0.0.1:
        url: "(( secValue `.env.database.credentials.secret` `url` ))"
        schema: "(( fromFile `./sql/schema.sql` | toYAML | hindent 6 ))"
        seed: "(( fromFile `./sql/seed.sql` | toYAML | hindent 6 ))"
      Redis@v0.0.1:
        noop: ""
  - staging:
      WebServer@v0.0.1:
        route:
          host: "(( .App ))-staging.(( .Domain ))"
